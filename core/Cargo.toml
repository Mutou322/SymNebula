[package]
name = "sym-nebula-core"
version = "0.1.0"
edition = "2021"
description = "NebulaMind — Pure mathematical formula deduction engine (no external deps)"

[dependencies]
symnebula-macros = { path = "../macros" }
zip = { version = "2", default-features = false, features = ["deflate"] }
