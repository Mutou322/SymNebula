[package]
name = "nebula-core"
version = "0.1.0"
edition = "2021"
description = "NebulaMind — Pure mathematical formula deduction and model dynamics engine"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1"
